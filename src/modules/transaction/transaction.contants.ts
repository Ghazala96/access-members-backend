export enum TransactionStatus {
  Processing = 'Processing',
  Completed = 'Completed',
  Failed = 'Failed'
}

export enum TransactionType {
  Payment = 'Payment'
}

export enum TransactionSrcAndDestType {
  VAccount = 'VAccount',
  Card = 'Card'
}
